var _jsx = function () { var REACT_ELEMENT_TYPE = typeof Symbol === "function" && Symbol.for && Symbol.for("react.element") || 0xeac7; return function createRawReactElement(type, props, key, children) { var defaultProps = type && type.defaultProps; var childrenLength = arguments.length - 3; if (!props && childrenLength !== 0) { props = {}; } if (props && defaultProps) { for (var propName in defaultProps) { if (props[propName] === void 0) { props[propName] = defaultProps[propName]; } } } else if (!props) { props = defaultProps || {}; } if (childrenLength === 1) { props.children = children; } else if (childrenLength > 1) { var childArray = Array(childrenLength); for (var i = 0; i < childrenLength; i++) { childArray[i] = arguments[i + 3]; } props.children = childArray; } return { $$typeof: REACT_ELEMENT_TYPE, type: type, key: key === undefined ? null : '' + key, ref: null, props: props, _owner: null }; }; }();

import React from 'react';
import Radium from 'radium';

var styles = {
  container: {
    padding: '10px',
    textAlign: 'center',
    margin: '20px auto'
  },
  btn: {
    margin: '0 10px',
    cursor: 'pointer'
  }
};

var RecipeBtns = function RecipeBtns(_ref) {
  var boards = _ref.boards,
      selectBoard = _ref.selectBoard;

  var renderBoards = function renderBoards() {
    return boards.map(function (board, i) {
      return _jsx('button', {
        onClick: function onClick() {
          return selectBoard(board);
        },
        className: 'btn btn-primary',
        style: styles.btn
      }, board.name + i, board.name);
    });
  };
  var boardBtns = boards.length > 0 ? renderBoards() : '';
  return _jsx('div', {
    style: styles.container
  }, void 0, boardBtns);
};

export default RecipeBtns = Radium(RecipeBtns);